{
    
    "title": "Event-schema",
    "description": "The arguments for the event that is triggered when a new event is registered.",
    "type": "object",

    "properties": {
        "event_name": {
            "type": "string"
        },
        "argument_schema": {
            "type": "string"
        }
    },
    "required": ["event_name", "argument_schema"]
}

