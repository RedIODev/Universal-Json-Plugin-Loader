{
    
    "title": "Endpoint-schema",
    "description": "The arguments for the event that is triggered when a new endpoint is registered.",
    "type": "object",

    "properties": {
        "endpoint_name": {
            "type": "string"
        },
        "argument_schema": {
            "type":"string"
        },
        "response_schema": {
            "type":"string"
        }
    },
    "required": ["endpoint_name", "argument_schema", "response_schema"]
}

