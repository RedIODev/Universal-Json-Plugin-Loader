{
    
    "title": "Endpoint-schema",
    "description": "Defines the shape of the event args",
    "type": "object",

    "properties": {
        "endpoint_name": {
            "type": "string"
        },
        "argument_schema": {
            "type":"string"
        },
        "response_schema": {
            "type":"string"
        }
    },
    "required": ["endpoint_name", "argument_schema", "response_schema"]
}

