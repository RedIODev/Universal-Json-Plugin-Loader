{
    
    "title": "Event-schema",
    "description": "Defines the shape of the event args",
    "type": "object",

    "properties": {
        "event_name": {
            "type": "string"
        },
        "argument_schema": {
            "type": "string"
        }
    },
    "required": ["event_name", "argument_schema"]
}

