{
    "title": "Power-response-schema",
    "description": "Defines the shape of the endpoint response",
    "type": "object",

    "properties": {
        "canceled": {
            "type": "boolean"
        },
        "state": {
            "enum": ["running", "shutdown", "restart", "cancel"]
        }
    },
    "required": []
}

